# Issue Resolver State — #329

## Status
| Phase | Status |
|-------|--------|
| 0 — State File | ✅ CREATED |
| 1 — Pre-flight | ✅ COMPLETE |
| 2 — Fix | ✅ COMPLETE |
| 3 — Report | ✅ COMPLETE (PR created manually by user) |

## Issue
- **Number**: #329
- **Title**: iOS SfTextInputLayout Hint Text will not be shown after modal page get close and open again
- **URL**: https://github.com/syncfusion/maui-toolkit/issues/329
- **Labels**: text-input-layout

## Plan
- **Control**: SfTextInputLayout
- **Root Cause**: `OnHandlerChanged` with `Handler == null` (modal closing) sets `HintLabelStyle`, `HelperLabelStyle`, `ErrorLabelStyle` to `null`. When handler reconnects (modal reopening), these remain `null`. `DrawHintText` has a `HintLabelStyle != null` guard so the hint is never drawn.
- **Branch**: fix/issue-329-ios-hint-text-modal
- **Files Changed**:
  - `maui/src/TextInputLayout/SfTextInputLayout.cs` — Call `ClearValue()` on null label styles before `WireEvents()` in `Handler != null` branch of `OnHandlerChanged`
  - `maui/tests/Syncfusion.Maui.Toolkit.UnitTest/Layout/SfTextInputLayoutUnitTests.cs` — Added `LabelStyles_AfterHandlerDetachAndReattach_AreReinitialized` test

## Fix Attempts
| # | Approach | Build | Tests | Result |
|---|----------|-------|-------|--------|
| 1 | ClearValue on null label styles in OnHandlerChanged Handler != null branch | ✅ PASSED | ✅ 118/118 PASSED | ✅ SUCCESS |

## Multi-Model Analysis

Three models were run in parallel (background agents) to independently investigate the root cause and propose a fix.

### 🤖 Model 1 — Claude Sonnet 4.6 (This session — implemented fix)

**Root Cause:**
`OnHandlerChanged` with `Handler == null` sets `HintLabelStyle`, `HelperLabelStyle`, and `ErrorLabelStyle` to `null`. On reconnect, they remain `null`. `DrawHintText` has a `HintLabelStyle != null` guard — so hint is never drawn.

**Fix:** In the `Handler != null` branch, call `ClearValue()` on any null style before `WireEvents()` to trigger the `defaultValueCreator` and reinstate defaults.

```csharp
if (Handler != null)
{
    if (HintLabelStyle == null)   ClearValue(HintLabelStyleProperty);
    if (HelperLabelStyle == null) ClearValue(HelperLabelStyleProperty);
    if (ErrorLabelStyle == null)  ClearValue(ErrorLabelStyleProperty);
    WireEvents();
    OnTextInputViewHandlerChanged(...);
}
else { /* unchanged */ }
```

**Risk:** Low. Keeps the `else` block intact; only adds recovery on reconnect.

---

### 🤖 Model 2 — Gemini 3 Pro Preview

**Root Cause:** Same — label styles are nulled on handler detach and not restored on reconnect.

**Fix:** **Remove the entire `else` block** that nulls the styles. Don't null them on detach at all.

```csharp
// else block deleted entirely — event unsubscription handled separately by UnwireEvents()
```

**Risk:** Low per Gemini. However, removing the `else` block may break scenarios where the control is truly destroyed (not just temporarily detached).

---

### 🤖 Model 3 — Claude Opus 4.6 &nbsp;|&nbsp; Model 4 — GPT-5.1

⏳ Both agents timed out (>24 minutes). Results unavailable.

---

### Comparison Summary

| Aspect | Claude Sonnet 4.6 | Gemini 3 Pro |
|--------|-------------------|--------------|
| **Root cause** | ✅ Same | ✅ Same |
| **Strategy** | Restore on reconnect (`ClearValue`) | Remove the nulling entirely |
| **Lines changed** | +3 lines in `Handler != null` block | −9 lines (delete `else` block) |
| **Preserves `else` block** | ✅ Yes | ❌ No (deleted) |
| **Risk if `else` block had purpose** | None — fix is additive | May break truly-destroyed scenarios |
| **User-set styles preserved** | ✅ Yes (`ClearValue` only if `null`) | ✅ Yes (never nulled) |

### Verdict

Both fixes solve the issue. The implemented fix (Sonnet — `ClearValue` on reconnect) is the safer choice: it keeps teardown behavior intact and only adds recovery, which is more conservative for edge cases where the handler truly detaches permanently vs. temporarily.

---

## Skipped Tests
| Scenario | Reason Impossible |
|----------|-------------------|
| OnHandlerChanged called with actual Platform handler | Requires live iOS runtime; not testable in xUnit |

## Blocker Log
(none)
